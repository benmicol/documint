= simple_form_for(@message) do |f|
	= f.hidden_field :account_id, :value => current_account.id
	= f.input :content, placeholder: "New Message", label: false
	= f.button :submit, class: "btn btn-primary"